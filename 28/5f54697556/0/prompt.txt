sur l'hote : Local go test fails on two tests (TestArchiveLocalIsWhatWasCommitted, TestArchiveRemoteFetchesABranchAndASHA) — Windows-only artifact: core.autocrlf=true on this host turns the fixture's committed "committed\n" into "committed\r\n" inside t.TempDir()'s fresh repo, which has no .gitattributes of its own to override it. CI runs on Linux where autocrlf defaults off, so this should be green there. Pushing and letting CI confirm rather than blocking on a host-specific quirk . créer un .gitattributes